<Window x:Class="DeskTop.Util.StatusIndicator"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DeskTop.Util"
        mc:Ignorable="d"
        Title="Загрузка" Height="100" Width="400">
    <Grid VerticalAlignment="Center" >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="80"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="35"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="{Binding CurName}" Margin="10,3"/>
        <TextBlock Text="{Binding Current}" Grid.Column="1" HorizontalAlignment="Right"/>
        <TextBlock Text="из" Grid.Column="2" Margin="3,0" />
        <TextBlock Text="{Binding Max}" Grid.Column="3" HorizontalAlignment="Left" Margin="0,0,5,0"/>
        <ProgressBar  Grid.Row="1" Grid.ColumnSpan="4" Margin="5" Value="{Binding Current}" Maximum="{Binding Max}"/>
    </Grid>
    
</Window>
